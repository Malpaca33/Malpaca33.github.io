### 1.反例：比如我将类命名为 BufferedReader
这会导致 Java 认为你要用你自己写的 BufferedReader，而不是 `java.io.BufferedReader` → 所以根本无法读取文件。

### 2.要分清去重时候使用的是Map还是Set
✔ LinkedHashSet  
✖ LinkedHashMap

### 3. throw new LinkedHashMap<>() 无法抛出
完全错误：
不能 throw 一个 Map
catch 不能抛出结构体，类型也不对

### 4. 写了 `map.put((Integer) line, line)会ClassCastException爆炸

`line` 是字符串，却强制转换为 Integer，会**直接爆炸**：
``` vbnet
ClassCastException: String cannot be cast to Integer`
```
